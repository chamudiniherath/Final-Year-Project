First Step:
Begin by opening the file and locating the Task Management System adorned with the distinctive elephant logo.

Second Step:
To access the system, login credentials are required. Enter "admin" as the username login and "admin" as the password.

For database connectivity, ensure to provide the necessary credentials within the connect.php file. Additionally, include the SQL file to facilitate smooth database integration.